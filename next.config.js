// next.config.js
const nextConfig = {
  eslint : {
    ignoreDuringBuilds : false,
  },
}
require('dotenv').config();

module.exports = {
  reactStrictMode: true,
};
// next.config.js
require('dotenv').config();

module.exports = {
  reactStrictMode: true,
};
